function sum(...a) {
    let result = 0;
    for (let i = 0; i < a.length; ++i)
    result += a[i];
    return result;
    }

function test_sum(f){
    let result= sum
    for(let j=0; j<6; j++){
        a[i]=(Math.floor(Math.random()*(10-1))+1);
     }
}

for(let k=0; k<6; k++){
    console.log(test_sum)
}